#include <stdio.h>

int main(){

    //Criando as variáveis a e b que serão as parcelas
    int a,b;


    // Entrada das parcelas
    scanf("%d %d", &a, &b);

    //realizando a soma
    int soma = a + b;


    // Mostrando a soma
    printf("SOMA = %d\n", soma);

}